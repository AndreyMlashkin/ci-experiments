name: Release and tag
permissions:
  contents: write
on:
  push:
    branches:
      - master
      - main

jobs:
  release-and-tag:
    name: Call reusable set version
    uses: NilFoundation/ci-cd/.github/workflows/reusable-set-version.yml@9911d9eaea342bb3bd603520d7625faebea85d71
    secrets: inherit
