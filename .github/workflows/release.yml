name: Release and tag
permissions:
  contents: write
on:
  push:
    branches:
      - master
      - main

jobs:
  release-and-tag:
    name: Call reusable set version
    uses: NilFoundation/ci-cd/.github/workflows/reusable-set-version.yml@a86452fb28a8051e3bdd7fddbea6b807a20b84fa
    secrets: inherit
