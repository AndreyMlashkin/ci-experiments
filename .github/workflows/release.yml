name: PR Testing

on:
  pull_request:
  push:
    branches:
      - master

jobs:
  handle-syncwith:
    if: github.event_name == 'pull_request'
    name: Call reusable set version
    uses: NilFoundation/ci-cd/.github/workflows/reusable-set-version.yml@930e6a9d0147909b2385c227464373b907c78cc3
    secrets: inherit

