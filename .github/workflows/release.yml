name: Release and tag

on:
  push:
    branches:
      - master
      - main

jobs:
  release-and-tag:
    name: Call reusable set version
    uses: NilFoundation/ci-cd/.github/workflows/reusable-set-version.yml@ea4946a67bca78839aa0c58dad5f129250919888
    secrets: inherit
