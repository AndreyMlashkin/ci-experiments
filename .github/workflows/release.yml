name: Release and tag
permissions:
  contents: write
on:
-  push:
-    branches:
-      - master
-      - main

jobs:
  release-and-tag:
    name: Call reusable set version
    uses: NilFoundation/ci-cd/.github/workflows/reusable-set-version.yml@2dcc448d2dd334bcc3cbe527dee75c56910a091c
    secrets: inherit
