name: Release and tag
permissions:
  contents: write
on:
  push:
    branches:
      - master
      - main
  pull_request:
    types: [edited]

jobs:
  release-and-tag:
    name: Call reusable set version
    uses: NilFoundation/ci-cd/.github/workflows/reusable-set-version.yml@f151695b0a9d2d63585cf8f3afd1b728ded20129
    secrets: inherit
