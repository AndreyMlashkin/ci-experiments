name: Release and tag

on:
  push:
    branches:
      - master
      - main

jobs:
  release-and-tag:
    name: Call reusable set version
    uses: NilFoundation/ci-cd/.github/workflows/reusable-set-version.yml@e2be1c85314c2b1be74f4aecd4a7484ca1f28500
    secrets: inherit
